
# Database configuration
# Set DATABASE_TYPE to "postgres" or "sqlite"
DATABASE_TYPE=sqlite
# SQLite configuration (used when DATABASE_TYPE=sqlite)
SQLITE_DATABASE_PATH=data/app.db
# PostgreSQL configuration (used when DATABASE_TYPE=postgres)
# Replit will set DATABASE_URL automatically when using Replit Database
# For local development with PostgreSQL, set this to your local PostgreSQL URL
# DATABASE_URL=postgresql://username:password@localhost:5432/dbname
